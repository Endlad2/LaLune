[package]
name = "lalune-openwrt"
version = "0.1.0"
edition = "2021"

[dependencies]
tiny_http = "0.12"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4"] }
local-ip-address = "0.6"

[profile.release]
opt-level = "z"
lto = true
strip = true
